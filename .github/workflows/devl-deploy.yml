name: Devl Deploy

on:
  workflow_dispatch:
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: devl
    steps:
      - name: Get AWS Credentails
        uses: aws-actions/configure-aws-credentials@v1
        with:  
          aws-access-key-id:  AKIAWCZC54TYBMWUDLO7
          aws-secret-access-key: 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
          aws-region: us-east-1
      - name: Assume Role
        run: |
          ROLE_ARN="arn:aws:iam::418295702768:role/LambdaRoleAdminAccess"
          ROLE_SESSION_NAME="workflowHandlerSession"

          CREDS=$(aws sts assume-role --role-arn $ROLE_ARN --role-session-name $ROLE_SESSION_NAME)
      - name: Test AWS Role
        run: aws s3 ls
